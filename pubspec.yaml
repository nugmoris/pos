name: pos
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.0.6 <4.0.0"

dependencies:
  win32: ^5.0.9
  http: ^1.2.1
  android_id: ^0.4.0
  intl: ^0.20.1
  barcode_scan2: ^4.3.0
  bluetooth_print_plus: ^2.4.6
  flutter_launcher_icons: ^0.14.0
  google_fonts: ^6.2.1
  riverpod: ^2.5.3
  flutter_riverpod: ^2.5.3
  #android_intent_plus: ^5.3.0
  open_file: ^3.5.10
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.3
  dio: ^5.4.0
  #android_package_installer: ^0.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter_icons:
  android: true
  ios: true
  image_path: "assets/icon/buy.png"

  flutter_lints: ^2.0.0

flutter:
  assets:
    - assets/birukecil.png
    - assets/birumuda.png
    - assets/biru2.png

  uses-material-design: true
